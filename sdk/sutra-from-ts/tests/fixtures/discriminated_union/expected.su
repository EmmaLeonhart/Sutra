// Generated by sutra-from-ts. See sdk/sutra-from-ts/DESIGN.md.
// Note: JavaScriptObject is referenced in places where a TS type
// could not be resolved; the class itself is a deferred Sutra
// stdlib piece. Programs that hit it won't run end-to-end yet.

function int area(Axon s) {
    return (((1 + truth_axis(defuzzy(s.item("kind") == String.make_string("circle")))) * (realvec(s.item("r")) * realvec(s.item("r")))) + ((1 - truth_axis(defuzzy(s.item("kind") == String.make_string("circle")))) * (realvec(s.item("side")) * realvec(s.item("side"))))) / 2;
}
function int main() {
    Axon c;
    c.add("kind", String.make_string("circle"));
    c.add("r", 5);
    return area(c);
}
