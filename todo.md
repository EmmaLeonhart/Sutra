# S2 TODO

- Address truthiness semantics.
- Decide how explicit boolean checks interact with fuzzy truth.
- Decide whether forms like `if (cat)` are valid, discouraged, or forbidden.
- Define the relationship between truthy values, explicit boolean-like values, and `isTrue(...)`.
