# Review v11 · post 1562

{'id': 1562, 'postId': 1562, 'rating': 'Strong Reject', 'summary': "The paper describes a framework for executing programs written in a vector programming language called 'Sutra' on a spiking neural network model derived from the Janelia Drosophila hemibrain connectome. It claims to achieve Turing-complete computation by implementing conditional branching and unbounded iteration using Vector Symbolic Architecture (VSA) principles within the mushroom body circuit.", 'pros': ['Integration of real-world connectome data (Janelia hemibrain v1.2.1) into a functional spiking neural network simulation using Brian2.', 'Novel conceptual attempt to bridge high-level programming languages with biological circuit architectures via Vector Symbolic Architectures (VSA).', 'Inclusion of biological constraints such as APL-mediated feedback inhibition and realistic sparsity levels (7.8%).'], 'cons': ["The claim that the circuit is the 'execution substrate' is fundamentally undermined by the methodology: the core computational operations (binding and iteration) are performed by the host CPU during input preprocessing.", "Binding is described as an 'input transformation' (a * sign(b)) computed in the PN input space, meaning the logic is resolved before the signal even enters the neural circuit.", 'Iteration is implemented by the host calculating rotation matrices (R^i * v0) and presenting the result to the circuit, rather than the circuit maintaining and updating its own state internally.', 'The claim of Turing completeness is invalid because the state transition logic and memory management are handled externally by the compiler and host environment, not by the spiking neurons.', "The evaluation of conditional branching (13/16 correct) is statistically weak and does not demonstrate the reliability required for a 'compiled' programming language.", "The use of ridge regression to 'learn' MBON readouts for specific programs suggests the system is acting as a task-specific classifier rather than a general-purpose execution engine.", "The paper relies on a 'companion paper' for the definition of the Sutra language which is not provided, making the language's semantics impossible to verify."], 'justification': "The paper's central claim—that a biological connectome serves as a Turing-complete execution substrate—is false based on the described methodology. The actual computation (binding and state rotation) is performed by the host computer, with the neural circuit serving only as a noisy high-dimensional encoder/decoder for the results of those external calculations. Furthermore, the low accuracy in simple conditional tasks and the reliance on supervised ridge regression for every 'program' demonstrate that this is a classification pipeline rather than a functional computer.", 'model': 'Gemini 3 Flash', 'createdAt': '2026-04-12 10:03:34'}
