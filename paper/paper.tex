% paper.tex — Sutra paper LaTeX wrapper.
%
% The substantive paper content lives in paper/paper.md. The pages.yml
% workflow runs `pandoc paper.md -t latex -o paper.tex.body` before
% latexmk, so the entire body of the markdown lands here at \input
% time. paper.tex itself is a thin wrapper that supplies the
% documentclass, packages, anonymization switch, title, and author
% block.
%
% Two builds from the same source via the \ifanon switch:
%   pdflatex paper.tex                                    -> named (full)
%   pdflatex -usepretex='\newif\ifanon\anontrue' paper.tex -> anonymized
%
% Build chain:
%   pages.yml  -> pandoc paper.md -t latex -o paper.tex.body
%              -> latexmk -pdf paper.tex      (named)
%              -> latexmk -pdf -usepretex=... paper.tex (anonymized)
%              -> docs/paper.pdf staged for the GitHub Pages deploy

\documentclass[11pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{hyperref}
\usepackage{url}
\usepackage{xcolor}
\usepackage{geometry}
\geometry{margin=1in}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{caption}
\usepackage{enumitem}
% pandoc's tightlist macro for compact lists:
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
% pandoc's pandocbounded for image sizing:
\providecommand{\pandocbounded}[1]{#1}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  citecolor=blue,
}

% Anonymization switch for NeurIPS double-blind. Build with
% `-usepretex='\newif\ifanon\anontrue'`. Default is named.
\makeatletter
\@ifundefined{ifanon}{\newif\ifanon}{}
\makeatother

\ifanon
  \title{Sutra: A Programming Language for Vector-Symbolic\\
    Computation in Vector Embedding Spaces}
  \author{Anonymous Authors}
\else
  \title{Sutra: A Programming Language for Vector-Symbolic\\
    Computation in Vector Embedding Spaces}
  \author{Emma Leonhart\\
    \texttt{EmmaLeonhart999@gmail.com}}
\fi

\date{}

\begin{document}
\maketitle

% The paper body comes from `pandoc paper.md -t latex -o paper.tex.body`,
% generated by the pages.yml workflow before latexmk runs. Everything
% below this line — Abstract, sections 1-5, references — is sourced
% from paper.md.
\input{paper.tex.body}

\end{document}
